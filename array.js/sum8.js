// Using `forEach()`  
// Write a program that uses the `forEach()` method to print each
//  element in an array of integers.

let friends=["senthil","nathan","akshu"];
friends.forEach (function(geek){
    console.log(geek);
})