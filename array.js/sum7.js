// Using `for...of`  
// Write a program that uses the `for...of`
//  loop to print each element in an array of strings.


let squads = ["saravans", "senthil", "jo"];
for (let squad of squads) {
    console.log(squad);
}

