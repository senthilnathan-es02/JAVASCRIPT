// Joining Array Elements Without a Separator
// Create an array of 4 fruits. Use join() without any separator to combine the array
//  elements into a single string and print the result.

let array =["apple","orange","banana"];
let answer=array.join()
console.log(answer);