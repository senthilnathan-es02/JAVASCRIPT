//  Extract a Portion of a String Using slice()
// Write a program that extracts a portion of a string from the
//  4th index to the 8th index using slice().
// Example:
// Input: "Hello, World!", Slice from index 4 to 8
// Output: "o, W"

let A = "Hello, World!";
function result(){
    console.log(A.substring(4,8));
}
result();
